server.c
  socket() 함수 : 소켓을 생성한다.
  bind() 함수 : 주소를 바인딩 한다.
  listen() 함수 : 클라이언트의 접속을 기다린다.
  accept() 함수 : 클라이언트의 접속을 수락한다.
  send, recv() 함수 : 클라이언트와 데이터를 주고 받는다.(상호작용)
  closesocket() 함수 : 클라이언트와의 연결을 종료한다.
  
client.c
  socket() 함수 : 소켓을 생성한다.
  connect() 함수 : 서버에 접속요청을 한다.
  recv, send() 함수 : 연결이 된 후부턴 서버와 데이터를 주고 받는다.(상호작용)
  closesocket() 함수 : 연결을 종료한다.
